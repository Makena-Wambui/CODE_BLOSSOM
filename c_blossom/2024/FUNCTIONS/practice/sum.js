/*
Create a function called `addNumbers` that takes two parameters (`num1` and `num2`) and
returns their sum.
*/

function addNumbers(num1, num2) {
  return num1 + num2;
}

console.log(addNumbers(2, 3)); // 5
