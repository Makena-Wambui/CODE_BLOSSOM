// You can decalre variables to unpack values using the destructuring assignment syntax

const { bar } = foo;

/*
The above statement creates a variable called bar from the object foo.
We assign to it a value that is the same as the key/property bar in the object foo.
*/
