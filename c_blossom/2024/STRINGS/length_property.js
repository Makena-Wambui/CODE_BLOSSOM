// length => string length
// Length is a numeric property not a function so it doesn't have parentheses.

console.log(`Hello`.length); // 5

console.log("Hello\n".length); // 6 because \n is a single special character
