// Instead of using methods like push(), splice(), concat...
const parts = ["shoulders", "knees"];
const lyrics = ["head", ...parts, "and", "toes"];

// Just like in function argument lists, ... may be used anywhere in the array literal, and multiple times

console.log(lyrics);
